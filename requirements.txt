# Core simulation and control
mujoco==3.3.5
numpy==2.3.2
matplotlib==3.10.8
gymnasium==1.1.1  # Added since you're using it
stable_baselines3==2.7.1
torch==2.8.0

# Video/rendering support (optional, only if needed)
opencv-python==4.13.0.92
mediapy==1.2.6
moviepy==2.2.1

# Data handling and utilities
pandas==3.0.0
tqdm==4.67.1
python-dotenv==1.2.2

# ROS2-specific packages (only if you actually need ROS2 integration)
# Note: Most ROS2 packages should be installed via apt or rosdep, not pip
# rclpy==7.1.9
# ros2topic==0.32.8
# sensor-msgs==5.3.6
# visualization-msgs==5.3.6
# tf2-ros-py==0.36.19